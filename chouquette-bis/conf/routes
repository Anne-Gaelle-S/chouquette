# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Extractor
GET     /extract                    controllers.Extractor.extract(text: String)
GET     /geolocalise                controllers.Geolocalisator.geolocalise(places: String)
GET     /zonator                    controllers.UTMZonator.zonator(coordonnees: String)
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

POST    /zonator                    controllers.UTMZonator.zonatora